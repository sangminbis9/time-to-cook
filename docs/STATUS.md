# 구현 상태 (2026-07-17)

## 완료 — 수직 슬라이스 (PLAN.md §29)

| 단계 | 내용 | 검증 |
|---|---|---|
| P0 | Godot 4.7 부트스트랩, 픽셀 퍼펙트(640×360), 아트 생성기, Galmuri 폰트 | headless import + GUT |
| P1 | 코어 순수 로직 + 정의 데이터(.tres) | 단위 테스트 56개 |
| P2 | 매장 씬(ASCII 레이아웃), 이동, 대상 선택·하이라이트, ENet 호스트 권한 | `simultaneous_pickup` |
| P3 | 설비 5종+재료함, 닭강정 파이프라인, 조리 상태 4종 | `coop_cook_submit` |
| P4 | 주문 스폰·제출·매출, 하루 루프, 마감 폐기, 정산 | `day_loop` |
| P5 | 냉장고 UI·단독 잠금·보관 검증·야간 보존 | `fridge` |
| P6 | 버전드 JSON 세이브, 자동 저장, 타이틀/참가 플로우 | `save_write`+`save_load`, `host_quit` |
| P8 | 전처리 직원: 고용, 서버 FSM(재료함→도마→칼질→출력), 작업 독점(§10.6), 일급 | `employee` |
| P9 | 재료 수동 주문(잔여 마감 폐기 §21.1), 메뉴 가격 설정, 대출(일일 이자·전액 상환) | `economy` |
| P10 | 한국 8+일본 8 도시, 도시 지도(개설/이동), 매장별 상태 분리(번들 스왑), 자동화 매출·전 매장 임대료, 세이브 v2 | `multi_store` + `test_cities` |
| P10b | 시장 정보(§7): 정보상 2종+현지 자문, 등급별 공개 항목, 오차·사기(전액 손실·기존 정보 유지), 스냅샷·경과일·재조사 배지, 업그레이드 할인, 인천 무료 최고 등급 | `market` + `test_market_report` |
| P10c | 동적 경제(§8.1): 유효 수요(수요×배율÷경쟁)→주문 빈도·자동화 매출, 가격 민감도→수용률(폭리 시 주문 0), 일일 ±5% 드리프트(재조사의 실질 근거), 보고서에 배율 반영 | `dynamic_economy` + `test_city_economy` |
| P10d | 급격 경제 이벤트(§8.1/§23.2): 호황·불황·관광 급증·공급 충격 4종, 일일 발생(8%)·지속·만료, 수요/재료비 배율, 공개 표시(§7.1), 세이브 포함 | `econ_events` + `test_econ_events` |
| P8b | 직원 확장(§10.2~10.4): 등급 D~S 가중 무작위 후보 3명/일, 특성 5종(성실·무난·재빠름·게으름·병약), 채용 시 스탯 영구 고정, 30일 휴가 사전 확정(휴가일 결근·급여 지급), 최소 근무일·해고 위약금 | `employee_roster` + `test_employee_roster` |
| P11 | 독립 매장 이동(§6): 피어별 `request_travel`, 도시별 라이브 매장 다중화(`LiveStore` — 그리드·설비·냉장고·주문·직원·재고·매출), 매장 스코프 RPC에 도시 라우팅, 빈 매장 자동 오프라인 번들화, 다른 도시 플레이어 숨김, 세이브 v3(+v2 마이그레이션) | `independent_stores` + `test_save_roundtrip` |
| P12 | 매장 이벤트(§23.1/§23.3): 화재(일일 판정 + 탄 음식 35% 점화, 튀김기 아이템 소실, J 연타 3회 진압)·정전(튀김기 조리 정지, 냉장고 옆 차단기 J 복구, 화면 어두움), 이벤트 중 주문 정지(손해는 대응 속도에 비례), HUD 배너·프롬프트·화재 설비 붉은 표시 | `store_events` |
| P13 | UI·조작 1차(§14/§15): 매장 축소 13×9+양옆 벽 제거(맵 중앙 고정 카메라), 설비 배치 동적화(`LiveStore.placements`)+준비 단계 이동·구매(클릭 배치, 스폰·점유·플레이어 발밑 거부), 근접 기준 상호작용 선택(동거리는 시선 우선), 캐릭터 1.5배·인벤토리 슬롯 2배, 우측 상단 주제 버튼(매장/직원/경영/지도)→팝업 UI, 냉장고 UI는 냉장고 슬롯만 표시(플레이어 쪽은 HUD 하단 바 클릭으로 이동/교환) | `station_edit` |
| P14 | 비주얼 1차(§3): 절차 생성 FX 텍스처(`tools/gen_fx_art.py` → sprites/fx/) — 캐릭터·직원·설비·바닥 아이템 그림자, 앰비언트(CanvasModulate)+천장 조명 5점(PointLight2D, 정전 시 소등)+튀김기 온기 글로우, 비네트 오버레이(HUD 아래), 배경 클리어 컬러 웜 다크 | 스크린샷 검수 |
| P15 | 직원 역할(§10.1): 조리(출력대→튀김옷 b_2→빈 튀김기 동적 선택→NORMAL 회수→선반 c_3)·서빙(선반→제출대 주문 완료·매출) 추가, 후보 역할 무작위 고정·매장당 역할별 1명, 설비 점유를 위상→키 유도로 일반화(`_reserved_keys`), 역할별 스프라이트 톤 | `employee_roles` |
| P16 | 이벤트 확장+예방 설비(§23.1/§23.4): 누수·미끄러운 바닥(무작위 설비 대상, J 연타 3회 수리·청소, 미끄러움은 이동 40% 감속), 예방 설비 4종(스프링클러·발전기·배수·미끄럼 방지 — 매장 관리 팝업 구매, 보유 시 해당 이벤트 원천 차단, 매장 귀속 직렬화) | `prevention` |
| P17 | 양념 메뉴(§19.1/§19.6): 양념대(구매 설비 8000원, K 한 번으로 후라이드→양념 변환)·양념 닭강정 레시피(기본 3800원), 주문 스포너가 매장별 제조 가능 메뉴에서 무작위 선택(양념대 보유 매장만 양념 주문), 경영 팝업 가격 조정 전 메뉴 일반화 | `sauce_menu` |
| P18 | 대출 3건·만기(§9): 상품 3종(소액 3만/중액 5만/거액 10만 — 이자율·만기·만기 이자율 차등, `LoanBook` 순수 로직), 활성 3건 한도, 일일 이자 자동 납부(원금 불감), 만기일 정산 원금+만기 이자 일괄 납부, 자금 부족 시 연체(이자 +1%p 가산·신규 대출 제한), 중도 상환은 원금만, 구세이브 단일 대출 마이그레이션 | `loans` + `test_loan_book` |
| P19 | 광고(§8.3): 상품 2종(전단지 5000원/3일/수요 ×1.3, 지역 방송 20000원/5일/×1.6), 도시당 동시 1건, 유효 수요(주문 빈도·오프라인 자동화 매출)에 배율 곱, 매일 잔여 일수 감소·만료, 경영 팝업 집행·진행 표시, 세이브 포함 | `ads` + `test_city_economy` |
| P20 | 직원 재배치·질병(§10.4/§10.5): 준비 단계 개설 매장으로 즉시·영구 이적(운송비 동일 국가 3000/국가 간 8000, eid·계약 유지, 역할별 1명 검사, 오프라인 번들 직접 이적), 영업 시작 시 질병 판정(특성별 확률: 병약함 12%·성실함 1%·기본 3%) — 병가 종일 결근·조퇴 영업 후반 이탈·급여 지급 유지, 직원 팝업 재배치 UI·병가 표시 | `staff_transfer` |
| P21 | 도시별 매장 레이아웃(§6.6): 개설비 기준 3티어 템플릿(≤6만 소형 12×8 / ≥9만 대형 15×10 / 그 외 표준 13×9 — 설비 키 집합은 동일해 직원 고정 경로 항상 유효), `GameServer.layout`을 도시별 캐시(`layout_of`)로 전환, 매장 이동 시 지형·조명·카메라 리밋 재구축 | `city_layouts` + `test_store_layout` |
| P22 | 캐릭터 스킬(§11): CharacterDef 2종(미트=전처리 전문: 칼질 1회당 2회 진행+액티브 '집중 손질' 추가 +2 / 살구=운반 전문: 이동 +15%+액티브 '질주' ×1.5), L키 액티브(8초 지속·20초 쿨다운, 영업 중 전용·정산 리셋), 영구 업그레이드 2단계(공용 자금, 레벨당 지속 +3초, 환불 없음), 캐릭터 팝업·HUD 스킬 상태 표시 | `character_skill` |
| P23 | 급여·임대료 변동 연동(§8.1): 비용 배율 `cost_mult`(수요 변동의 절반이 물가 반영, §8.1 점진 변화), 임대료 = 도시별 `rent_per_day × cost_mult`(정산·지도 표시 공용 `current_rent`), 신규 채용 후보의 제시 급여·채용비에 전 도시 평균 물가(`avg_cost_mult`) 반영 — 재직자 급여는 §10.2대로 채용 시점 영구 고정 | `dynamic_economy` + `test_city_economy` |
| P24 | 정보상 재등장 로테이션(§7.3): 사기 발생 시 해당 정보상 2~4일 잠적(거래 차단·"연락 두절" 표시) 후 다른 이름으로 재등장(`MarketReport.scam_vanish`/`broker_gone`/`broker_name`, 별칭 풀 8종) — 가격·정확도·사기 확률은 §7.3대로 영구 고정, `FranchiseState.broker_state` 세이브 포함 | `market` + `test_market_report` |
| P25 | 캐릭터 정보 능력(§7.2-③): 살구 '발품 정보망' — 도시 지도에서 선택 도시의 2등급 정보를 무료 획득(3일 쿨다운, 사기 없음), `CharacterDef.info_source`로 정보 경로 참조, 무료분은 paid_total 미포함이라 상위 등급 할인에 반영 안 됨(§7.6), 사용일은 `FranchiseState.char_info_day` 세이브 포함, 캐릭터 팝업 능력 표시 | `char_info` + `test_market_report` |
| P26 | 연구 트리(§20): `ResearchDef` 5종(브랜드 강화·양념 소스 개발→양념대 해금·매장 안전 설비→예방 설비 해금·방송 마케팅→지역 방송 해금·일본 진출→일본 개설 해금, 선행: 브랜드→일본), 전체 트리 공개·공용 자금+연구 포인트(정산마다 +1 적립)·즉시 적용, 서버 게이트 4곳(설비/예방/광고/개설), HUD [연구] 팝업(카테고리·비용·선행·완료 표시), `FranchiseState.research`/`research_points` 세이브 포함 | `research` + `test_research` |
| P27 | 보험(§23.4): 매장 단위 가입·해지 토글(매장 관리 팝업, 준비 단계), 일일 보험료 500원·그날 매장 이벤트 발생 건당 보험금 2000원을 정산에서 반영(`LiveStore.events_today` 서버 휘발성 카운트), 가입 상태는 `preventions`의 "insurance" 키로 직렬화 공유, 정산 화면 보험료·보험금 행 표시 | `insurance` |
| P28 | 지원 역할 4종(§10.1): 청소(누수·미끄러움 자동 대응)·정비(화재 진압·차단기 복구 자동) — 이벤트 대상으로 이동 후 플레이어 연타와 같은 경로(`_server_event_hit`)로 처리(협력 가능, Phase TO_EVENT/FIXING), 계산(존재 시 제출 매출 ×1.05)·매니저(존재 시 매장 직원 작업 간격 ×0.9) 패시브, 후보 풀 7종·역할별 1명 유지, 역할별 스프라이트 톤·직원 팝업 라벨 | `employee_support` |
| P29 | 역할별 다수 고용: 역할당 1명 제한 제거(채용·재배치), 매장 총원 상한 8명(`MAX_EMPLOYEES_PER_STORE`, 초과 시 "store_full") — 같은 역할 다수의 동시 작업은 기존 설비 예약 유도(§10.6, IDLE 진입 시 예약 검사)가 그대로 조정 | `employee_roles` |
| P30 | 양념 변형 2종(§19.1): 매운(3900원)·간장(3700원) 닭강정 — 변형별 전용 양념대 설비(각 8000원, 기존 양념대와 동일한 K 변환 구조), 연구 '매운 양념 개발'·'간장 소스 개발'(각 8000원+1RP, 선행: 양념 소스 개발)로 구매 해금, 판매 메뉴 판정을 `SAUCE_TABLE_RECIPES` 매핑으로 일반화(스포너·가격 조정 자동 연동) | `sauce_menu` + `test_sauce_tables` |

## §29.4 완료 조건 체크리스트

- [x] 같은 주문 중복 제출 불가 — 주문 원자 완료 (`coop_cook_submit`, `test_orders`)
- [x] 동시 줍기 복제 불가 — 서버 직렬 처리 (`simultaneous_pickup`)
- [x] 아이템 상태 저장·동기화 — iid 기반, 스냅샷=세이브 동일 직렬화기
- [x] 준비·영업·마감 루프 무중단 — 2회 연속 (`day_loop`)
- [x] 냉장고만 익일 보존, 맵 위 아이템 마감 폐기 (`fridge`, `day_loop`)
- [x] 저장 후 재실행 → 동일 상태 복구 — 별도 프로세스 (`save_load`)
- [x] 입력 확장 가능 — InputMap 액션 기반 (게임패드는 액션에 이벤트만 추가)
- [ ] 32×32 픽셀 깨짐 없음 — **Windows 에디터에서 육안 확인 필요** (headless 불가)

## 다음 단계 (PLAN.md §30)

- 경제 확장: 연구 노드 추가(§20 — 물류·조리 기술 카테고리는 노드 없음,
  §21.2 공급업체 계약·자동 발주 등)
- 콘텐츠: 양념 변형 추가(단맛·매운맛·간장 완료 — P17/P30 / 마늘·지역 변형 잔여, §19.1),
  매장 이벤트 확장(화재·정전·누수·미끄러움 완료 — P12/P16 / 환기·장비 고장 등 잔여, §23.1),
  사운드(§34), 캐릭터 3인째 이상 추가(현재 미트·살구 2종, 각 액티브 스킬 1개는 완료 — P22, §11)
- 아트: 1차 외주분 24종 반영 완료 (`tools/import_art.py`로 다운스케일·배경 제거,
  원본은 `art_src/`). 타일 벌어짐은 tileset.tres의 `texture_region_size` 미지정
  (기본 16×16이라 타일의 1/4만 그려짐) 수정 + `tools/fix_tiles.py`(이음선 크롭·
  미러 변형·경계 혼합·벽 캡 어둡게)와 설비 가로 채움으로 해결. 렌더링 검수는
  `--nettest=host --scenario=screenshot`(화면 필요, headless 불가)로 스크린샷 촬영.
  재생성 필요분은 `docs/ART_SPEC.md`의 ChatGPT 프롬프트 참고
  (필수: station_submit·player_employee)

## 의도적 단순화 (슬라이스 범위)

- 캐릭터 선택 씬 없음 — 피어별 자동 배정 (호스트=미트/게스트=살구, §11.1의
  선택·중복 방지는 자동 배정으로 대체). 스킬 상태는 영업일 단위 리셋(재접속 저장 생략)
- 세이브 슬롯 1개 고정 (SaveSelect 씬은 P8+)
- 튀김기 스왑은 조리 중(덜 익음)에만 금지 — §15.2 준수
- 효과음·배경음 미구현 (§34는 이후 단계)
- 재고: 1일차만 기본 40개 제공(온보딩), 이후 준비 단계에서 500원/개 주문
- 대출: 총부채 한도(신용도·자산 연동)와 매각 압박 루프(§9)는 미구현 —
  연체는 이자 가산·신규 제한만, 자금은 음수 허용
- 연구: 포인트 획득 경로는 명세에 없어 정산마다 1점 고정으로 단순화. 노드 5종
  (§20 카테고리 중 물류·조리 기술은 비어 있음), 브랜드 강화는 효과 없이 선행 전용
- 직원: 7역할 전종(§10.1), 역할별 인원 제한 없이 매장 총원 8명 상한. 조리·서빙은
  고정 경로(b_2 튀김옷·c_3 선반·x_1 제출대) 사용 — 같은 역할 다수는 예약 교대로
  동작(전용 설비가 하나면 병렬 이득은 제한적, 튀김기는 병렬). 계산·매니저는 존재
  효과만(FSM 없음 — 계산대 설비·손님 줄 없음)이며 다수여도 효과 비중첩, 청소·정비의
  위치 정합은 연출 수준(대응 판정은 서버 이벤트 상태 기준). 후보 3명/일에 7역할
  무작위라 원하는 역할이 안 뜰 수 있음.
  오프라인 매장 직원의 휴가·질병은 통계 매출에 미반영. 재배치 비용은 거리 대신
  국가 차등(도시 좌표 없음), 교육 결근(§10.4)은 미구현
- 다매장: 레이아웃은 개설비 기준 3티어 템플릿(도시 고유 배치는 아님),
  플레이어가 없는 매장은 오프라인 번들
  (잔여 재고 폐기 규칙 제외 — 추상화), 자동화 매출 = 직원 수×수요×800원.
  게스트는 세이브 로드·접속 시 호스트 도시에 합류 (세이브의 "2" 도시는 참고용)
- 매장 이벤트: 화재·정전·누수·미끄러움 4종, 라이브 매장 전용(오프라인은 무사고 추상화),
  매장당 동시 1건, 사전 경고 없음. 차단기는 전용 설비 없이 냉장고 상호작용으로 대체.
  미끄러움 감속은 플레이어만(직원 무영향), 예방 설비는 이벤트 무발생 방식(자동 진압 연출 없음),
  환기 고장·장비 고장·통로 막힘·비상구(§23)는 이후 단계.
  보험은 라이브 매장만 보험료·보험금 대상(오프라인은 무사고 추상화와 일관),
  보상은 실손 산정 없이 이벤트 건당 정액
- 양념 메뉴: 단맛·매운맛·간장 3종(§19.1의 마늘·지역 변형은 이후), 변형마다 전용
  양념대 설비(공용 스프라이트)·연구 해금(P26/P30), 아이템 아트는 후라이드와 공용
  (아트 별도 작업 대기)
- 설비 구매: 5종(작업대·도마·튀김옷 작업대·튀김기·양념대)·가격은 GameServer 상수 테이블,
  판매(되팔기)·회전 없음. 매장 양옆 벽은 맞는 아트가 준비될 때까지 생략
